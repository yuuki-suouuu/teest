# Mintlify 機能テストページ

このページは、Mintlifyの主な機能を試すために作成されました。

---

## 1. コードブロックの強化（シンタックスハイライトとコピーボタン）

Mintlifyは、言語を指定するだけで美しいハイライトとワンクリックでコピーできるボタンを自動で追加します。

```python
# Pythonのコード例
def hello_mintlify():
    message = "Hello, Mintlify World!"
    print(message)

# 関数を実行
hello_mintlify()